import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  buttonSkill: {
    padding: 20,
    backgroundColor: '#1f1e25',
    borderRadius: 50,
    alignItems: 'center',
    marginVertical: 10,
  },
  textSkill: {
    color: '#fff',
    fontSize: 18,
    fontWeight: 'bold',
  },
});
